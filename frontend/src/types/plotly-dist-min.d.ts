// plotly.js-dist-min ships no type declarations of its own -- it's the same
// runtime API as plotly.js (just pre-bundled/minified), whose types we get
// from @types/plotly.js wherever we actually reference Plotly's types.
declare module 'plotly.js-dist-min'
